{
  "name": "yakovlevyuri-com",
  "version": "1.0.0",
  "dependencies": {
    "next": "^6.0.0",
    "nprogress": "^0.2.0",
    "prop-types": "^15.6.1",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-ga": "^2.5.0",
    "styled-jsx": "^2.2.6"
  },
  "devDependencies": {
    "now": "^11.1.7",
    "prettier": "^1.12.1"
  },
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "NODE_ENV=production next start",
    "deploy": "now -e NODE_ENV=production --token $NOW_TOKEN --npm",
    "alias": "now alias --token=$NOW_TOKEN",
    "clear-deployments": "now rm --safe --yes yakovlevyuri-com --token=$NOW_TOKEN",
    "prettier": "prettier --single-quote --trailing-comma all --write '{src,pages}/**/*.js'"
  },
  "now": {
    "name": "yakovlevyuri-com",
    "alias": "yakovlevyuri.com"
  }
}
